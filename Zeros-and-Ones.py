import numpy as np

n = tuple(map(int, input().split()))
# for i in n:
arra_np_zero = np.zeros(n,dtype=int)
print(arra_np_zero)
arra_np_one = np.ones(n,dtype=int)
print(arra_np_one)
# arra_np_zero = np.zeros(n)
